version: '3.8'

services:
  # Next.js Blog Public Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - '3001:3001'
    environment:
      # Database - Connect to shared MongoDB
      # Set MONGODB_URI in .env file, or it will use defaults below
      # Format: mongodb://username:password@mongodb:27017/cptsd-cms?authSource=admin
      MONGODB_URI: ${MONGODB_URI:-mongodb://admin:changeme@mongodb:27017/cptsd-cms?authSource=admin}
    # MongoDB is provided by cptsd-cms deployment
    # No depends_on needed as it's on shared network
    healthcheck:
      test:
        [
          'CMD',
          'wget',
          '--no-verbose',
          '--tries=1',
          '--spider',
          'http://localhost:3001',
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - app-network
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M

  # Note: MongoDB is shared from cptsd-cms deployment
  # Connect to existing MongoDB service on shared network

networks:
  app-network:
    external: true
    name: cptsd-cms_app-network

